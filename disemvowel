function disemvowel(str) {
  let vowels = ['a','e','i','o', 'u','A','E','I','O','U'];
  let res = [];
  for (let i = 0; i < str.length;i++) {
    res.push(str[i]);
      }
    
  let result = res.filter(item => !vowels.includes(item));
  return result.join('');
}
